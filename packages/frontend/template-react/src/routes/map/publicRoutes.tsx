import { PathRoutes } from "../../types/RoutePath";
import { ROUTES } from "../enum/Routes";

export const publicRoutes: PathRoutes[] = [
    {
        path: ROUTES.LOGIN,
        component: <h1>LOGIN</h1>
    }
]